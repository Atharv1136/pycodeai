# Python packages required for the application
# Note: pygame and other GUI packages are auto-installed at runtime by the app
# Only including packages that install easily without system dependencies
numpy==1.26.2
pandas==2.1.4
